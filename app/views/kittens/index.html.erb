<h1>All Kittens:</h1>
<% @kittens.each do |kitten| %>
  <div class='info'>
    <%= render partial: "info", locals: { kitten: kitten } %>
    <%= link_to "Delete", kitten, data: { turbo_method: :delete } %>
  </div>
<% end %>
